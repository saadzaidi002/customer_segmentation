{
  "name": "customer-segmentation-deploy",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "cd frontend && npm install --legacy-peer-deps",
    "build": "cd frontend && npm run build"
  }
}
